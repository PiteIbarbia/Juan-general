function maxValue(valores) {
    const ganancias = [];
    let max = 0;
    for(var i = 0; i < valores.length; i++) {
        for(var j = i + 1; j < valores.length; j++) {
            if(valores[j] - valores[i]){}
        }
    }
}